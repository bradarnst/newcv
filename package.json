{
  "name": "mynewcv",
  "version": "1.0.0",
  "description": "Brad Arnst CV",
  "scripts": {
    "dev": "run-p dev:*",
    "dev:scss": "node-sass --watch --output tempcss src/styles",
    "dev:parcel": "parcel watch tempcss/main.css --dist-dir src/css",
    "dev:11ty": "eleventy --serve --quiet",
    "topdf": "node ./topdf.js",
    "build": "run-s build:*",
    "build:scss": "node-sass --output tempcss src/styles",
    "build:parcel": "parcel build tempcss/main.css --dist-dir src/css",
    "build:11ty": "eleventy",
    "build:pdf": "node ./topdf.js",
    "deploy:aws": "aws s3 sync _site s3://cv.nexusseven.com --size-only --exclude '.git/*' && aws cloudfront create-invalidation --distribution-id E1LF1P8TKLF8QZ --paths '/*'"
  },
  "author": "Brad Arnst",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.7",
    "postcss-cli": "^9.1.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "autoprefixer": "^10.4.2",
    "cssnano": "^5.0.8",
    "node-sass": "^7.0.1",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.4.7",
    "postcss-cli": "^9.1.0"
  }
}
